{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "build": {
          "cmds": ["mvn clean package -DskipTests"]
        },
        "start": {
          "cmd": "java -jar target/bulk-gym-0.0.1-SNAPSHOT.jar"
        }
      }
    }
  }
}
